# Pinned to compatible floors rather than exact versions. The previous exact
# pins mixed langchain 0.1 with a langchain-openai release built against
# langchain-core 0.3, which fails to import.
streamlit>=1.30
langchain-core>=0.3
langchain-openai>=0.2
openai>=1.40
python-dotenv>=1.0
databricks-sql-connector>=3.0
pandas>=2.0
bcrypt>=4.0
